---
name: zsh
description: Run zsh shell commands with full interactive environment (loads .zshrc, aliases, functions)
argument-hint: "<command to run>"
---

Execute the following command in an interactive zsh shell (loads .zshrc, aliases, functions):

```bash
zsh -i -c "$ARGUMENTS"
```

Command to run: $ARGUMENTS
