### Name
Quick Check

### Description
A simple single-step operation that runs one command.
This skill has exactly ONE execution step, so it can be
represented as a leaf execute task (not requiring group structure).

### Aliases
- quick check
- simple test
- fast verify

### Steps
- Run the quick verification

### Execution
- quickcheck --fast
