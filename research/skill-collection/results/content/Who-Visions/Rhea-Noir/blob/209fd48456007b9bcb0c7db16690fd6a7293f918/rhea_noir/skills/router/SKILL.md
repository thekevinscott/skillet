# Router Skill

Select appropriate AI model tier based on complexity.

## Actions

- `route` — Select model for query
- `list` — List available models

## Usage

```python
skill.execute("route", query="explain quantum computing", has_image=False)
skill.execute("list")
```
