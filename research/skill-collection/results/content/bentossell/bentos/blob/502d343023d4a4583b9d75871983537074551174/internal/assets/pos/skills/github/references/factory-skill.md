---
name: github
description: GitHub CLI (gh) for repo and notification data.
---

# GitHub CLI (gh)

This surface uses the installed `gh` CLI and assumes you are already authenticated.

```bash
gh auth status
gh api user
```
