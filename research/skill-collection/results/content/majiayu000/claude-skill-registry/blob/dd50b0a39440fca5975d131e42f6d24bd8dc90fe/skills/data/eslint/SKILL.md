---
name: ESLint
description: General rules for linting this project; use when handling lint errors.
---

Always run `eslint --fix` instead of just `eslint`.
Consider this skill whenever linting or addressing lint errors.