---
description: Apply analyzed permissions to .claude/settings.json
---

# Apply Permissions

Apply the recommended permissions to your project.

`!npx cc-permissions apply`

Confirm the permissions were written to `.claude/settings.json`.
