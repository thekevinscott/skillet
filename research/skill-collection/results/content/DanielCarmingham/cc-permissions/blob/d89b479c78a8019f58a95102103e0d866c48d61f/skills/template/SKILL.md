---
description: Generate permissions from specific templates (e.g., nodejs, git, python)
---

# Generate Template Permissions

Generate permissions for templates: $ARGUMENTS

`!npx cc-permissions template $ARGUMENTS --output summary`

Show the user what permissions will be generated and ask if they want to apply them.
