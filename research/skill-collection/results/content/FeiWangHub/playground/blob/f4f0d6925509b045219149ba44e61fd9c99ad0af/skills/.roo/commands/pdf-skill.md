# pdf-skill

Extract text and metadata from PDF files.

## Prerequisites

Install the `pypdf` library:

```bash
pip install pypdf
```

## How to Use

Run the extraction script on any PDF file:

```bash
python3 .shared/pdf-skill/scripts/extract_pdf.py "input.pdf"
```
