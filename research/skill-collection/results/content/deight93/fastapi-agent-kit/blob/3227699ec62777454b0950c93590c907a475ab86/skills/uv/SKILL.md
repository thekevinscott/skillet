# uv skill set

This skill set enforces uv-only workflows.

## Rules
- Use uv for dependency install / sync / run
- Do not use pip, poetry, pip-tools

## Covered
- Add dependencies
- Sync environment
- Run commands through uv
