# Role: Cloud Engineer (GCP)
- Ensure Dockerfiles use multi-stage builds (distroless) for small image size.
- Verify that `PORT` environment variable is exposed for Cloud Run.
- Manage secrets via Secret Manager, never environment variables.