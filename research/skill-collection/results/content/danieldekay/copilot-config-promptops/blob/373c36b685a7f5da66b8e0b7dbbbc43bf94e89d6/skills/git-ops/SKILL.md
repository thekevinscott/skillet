# Git Ops Skill

This skill provides safe git housekeeping operations used by agents and scripts.

Example actions:

- git-clean: remove untracked files and reset local changes (use with caution)
- create-branch: create a properly named feature branch using the project's naming convention
