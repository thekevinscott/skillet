# Test Filter
Runs project tests and returns ONLY the error messages, stripping success logs.
Use this to save tokens when debugging failures.

## Usage
/test-filter

## Implementation
$HOME/.claude/scripts/test-filter.js

