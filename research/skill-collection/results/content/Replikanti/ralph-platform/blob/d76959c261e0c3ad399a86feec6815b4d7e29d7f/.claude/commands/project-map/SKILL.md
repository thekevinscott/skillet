# Project Map
Lists the project file structure in a token-optimized TOON format.
Use this instead of `ls -R` to quickly understand the codebase layout.

## Usage
/project-map

## Implementation
$HOME/.claude/scripts/tree-toon.js

