# Trace Dependencies
Finds all files that import or depend on the specified file.
Useful for checking impact before modifying a file.

## Usage
/trace-deps <filename>

## Implementation
$HOME/.claude/scripts/trace-deps.js $1

